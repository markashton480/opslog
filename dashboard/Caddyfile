:8601 {
	# Reverse proxy API requests to backend
	handle /api/v1/* {
		reverse_proxy api:8600
	}

	# Serve static dashboard files
	handle {
		root * /var/www/html
		try_files {path} /index.html
		file_server

		# Default: no-cache for HTML shell (all SPA routes)
		header Cache-Control "no-cache, no-store, must-revalidate"

		# Override: cache hashed static assets aggressively
		@static path *.js *.css *.woff2 *.woff *.ttf *.svg *.png *.jpg *.ico
		header @static Cache-Control "public, max-age=31536000, immutable"
	}

	encode gzip

	# Security headers
	header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self' {$OPSLOG_CSP_CONNECT_EXTRA:}; frame-src 'self' {$OPSLOG_CSP_FRAME_EXTRA:}"
	header X-Content-Type-Options "nosniff"
	header X-Frame-Options "DENY"
	header Referrer-Policy "strict-origin-when-cross-origin"

	respond /health 200
}
